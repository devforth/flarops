services:
  db:
    image: postgres:15
    environment:
      POSTGRES_PASSWORD: pw
      POSTGRES_DB: appdb
  api:
    build: ./api
    ports: ["3000:3000"]
    environment:
      DB_HOST: db
    depends_on: [db]
