services:
  db:
    image: postgres:16
    environment:
      POSTGRES_PASSWORD: pw
      POSTGRES_DB: shopdb
  api:
    build: ./backend
    ports: ["3000:3000"]
    environment:
      DB_HOST: db
    depends_on: [db]
  frontend:
    build: ./frontend
    ports: ["8080:8080"]
